# vling 스타일 개인/팀용 채널 분석 툴 — 설계안

목표: vling처럼 **대규모 공개 서비스**를 만드는 게 아니라, 내가 추적하는 채널 수십~수백 개를 대상으로 **① 채널 비교 분석**과 **② 모니터링/알림**을 하고, 결과를 Claude 프로젝트 안에서 대화로 받아보는 구조입니다.

---

## 1. 전체 아키텍처 (4개 레이어)

```
[YouTube Data API v3]
        ↓ (매일 1회 크론)
[① 수집기 Collector] → 채널/영상 스냅샷을 DB에 적재
        ↓
[② 지표 엔진 Metrics Engine] → 트렌드 점수, 성장률, 참여율 계산
        ↓
[③ 알림 엔진 Alert Engine] → 임계치 초과 시 Slack/이메일 발송
        ↓
[④ 연동 레이어] → MCP 서버로 감싸서 Claude 프로젝트가 실시간 조회
```

개인/팀 규모라 vling처럼 국내 채널 전수 크롤링, 광고단가 추정, 부적합 채널 필터링 같은 무거운 기능은 필요 없습니다. **"내 워치리스트 안에서" 비교하고 감시하는 것**에 집중하면 됩니다.

## 2. 데이터베이스 (이전 명세에 watchlist·alert 테이블 추가)

```
channels           -- 채널 기본정보 (이전 문서 참고)
videos             -- 영상 기본정보 (이전 문서 참고)
channel_snapshots  -- 일별 시계열 (구독자수, 조회수, 영상수)

watchlist
  id (PK), channel_id (FK), label(예: "경쟁채널"/"내채널"), added_at

alert_rules
  id (PK), channel_id (FK), metric(예: subscriber_growth_pct),
  condition(예: "> 5"), window(예: "24h"), active(bool)

alerts_log
  id (PK), rule_id (FK), triggered_at, value, message, notified(bool)
```

## 3. 채널 비교 분석 기능

`compare_channels(channel_ids: [])` 형태의 API 하나면 충분합니다. 반환값 예시:

| 항목 | 채널A | 채널B | 채널C |
|---|---|---|---|
| 구독자 수 | | | |
| 최근 30일 평균 조회수 | | | |
| 참여율 (좋아요+댓글/조회수) | | | |
| 업로드 주기(일) | | | |
| 구독자 7일 증가율(%) | | | |
| 숏폼 비중(%) | | | |

- 이 표를 Claude가 받아서 "어떤 채널이 왜 앞서는지" 서술형으로 해석하게 하면 됩니다 (이전에 만든 프로젝트 지침의 "경쟁 벤치마킹" 섹션이 그대로 이 데이터를 소비하는 구조).
- vling의 "트렌드 점수"는 국내 전체 채널 대비 백분위(percentile)라서 재현이 어렵지만, **워치리스트 내부 상대 순위**로 대체하면 충분히 실용적입니다.
  - 간단 공식 예시: `트렌드점수 = 0.4×정규화(7일 구독자 증가율) + 0.4×정규화(7일 평균 조회수 증가율) + 0.2×정규화(업로드 빈도)` 후 워치리스트 내에서 0~100 백분위로 환산.

## 4. 모니터링/알림 기능

- `alert_rules`에 등록한 조건(예: "구독자 24시간 증가율 5% 이상", "특정 영상 조회수가 하루 만에 2배")을 크론이 스냅샷 적재 직후 체크.
- 조건 충족 시 `alerts_log`에 기록 + Slack Webhook 또는 이메일로 즉시 발송.
- **중요**: Claude 프로젝트(대화형)는 사용자가 먼저 말을 걸어야 응답하는 구조라 "알아서" 푸시 알림을 보내지는 못합니다. 그래서 실시간 알림은 Slack/이메일로 별도 발송하고, Claude 프로젝트에는 "오늘 무슨 알림 있었어?"라고 물으면 `alerts_log`를 조회해서 요약해주는 역할을 맡기는 조합이 현실적입니다.
- (참고: 만약 Anthropic 계정에서 예약 작업 기능을 쓸 수 있는 환경이라면, 매일 정해진 시간에 "오늘의 알림 요약해줘"를 자동 실행하는 스케줄도 구성할 수 있습니다.)

## 5. Claude 프로젝트 연동 방법

두 가지 선택지가 있고, 규모를 고려하면 **A안을 권장**합니다.

**A. MCP 서버로 감싸기 (권장)**
- 위 API(compare_channels, get_channel_snapshot, list_alerts 등)를 MCP(Model Context Protocol) 서버로 감싸서 Claude에 커넥터로 연결.
- 장점: Claude가 대화 중 실시간으로 최신 DB 값을 직접 조회 → 데이터가 절대 stale해지지 않음. "A랑 B 채널 비교해줘"라고만 말해도 즉시 최신 수치로 답변 가능.
- 단점: MCP 서버를 직접 개발/호스팅해야 함 (다만 이미 API를 만들고 있으니 MCP 래퍼 추가는 상대적으로 작은 작업).

**B. 정기 리포트 파일을 프로젝트 지식에 업로드**
- 크론이 매일 비교표/알림 요약을 마크다운으로 만들어 프로젝트 지식 파일을 교체.
- 장점: 구현이 훨씬 간단(서버-Claude 간 실시간 연동 불필요).
- 단점: 실시간성이 떨어지고, 파일을 수동/자동으로 계속 교체해줘야 함. "방금 이 채널 어때?" 같은 즉흥 질문엔 못 씀.

→ 지금 단계에선 **B로 MVP를 빠르게 검증하고, 워치리스트가 늘어나거나 실시간성이 중요해지면 A(MCP)로 전환**하는 흐름을 추천합니다.

## 6. 구축 로드맵 (단계별)

1. **1단계**: DB 스키마 + 채널 등록(watchlist) + 매일 스냅샷 크론 (이전 API 명세 문서 기반)
2. **2단계**: `compare_channels` API 구현 + 워치리스트 내 트렌드 점수 계산
3. **3단계**: `alert_rules` + 알림 발송(Slack/이메일) 파이프라인
4. **4단계**: 정기 리포트를 프로젝트 지식에 업로드하는 방식으로 Claude 연동 시작 (B안)
5. **5단계**: 워치리스트가 커지거나 실시간 질의가 잦아지면 MCP 서버로 전환 (A안)

---

### 참고 자료 (Sources)
- [vling 서비스 소개 (파이낸셜뉴스)](https://www.fnnews.com/news/202006081225168881)
- [vling 유튜브 마케팅 플랫폼](https://vling.net/en/home)
